# speedserver on Docker-Compose
speedserver ookla

Simple one click installation of a speedtest server. You only need to have Docker and Docker-compose installed.

Download docker-compose.yml file and run "docker-compose up"


